import React, { Component } from "react";

export default class CreateTaskPage extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return <p>This is the create task page</p>;
  }
}